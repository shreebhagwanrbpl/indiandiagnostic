export const metadata = {
  title: "Diagnostic & Lab Services | Indian Diagnostic",
  description: "Explore medical diagnostic equipment, installation, maintenance, and support services across India.",
};

export default function ServicesLayout({ children }) {
  return children;
}
